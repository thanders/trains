export default class Article {
  constructor(title, blurb, date, file, banner, bannerCopyWrite) {
    this.title = title;
    this.blurb = blurb;
    this.date = date;
    this.file = file
    this.banner= banner
    this.bannerCopywrite = bannerCopyWrite
    
  }

  trainStation(input){ this.trainStation = input }
}